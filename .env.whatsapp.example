# ============================================================================
# WhatsApp Cloud API Configuration
# ============================================================================

# Your WhatsApp Business Account credentials
WHATSAPP_ACCESS_TOKEN=your_access_token_here
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id_here
WHATSAPP_BUSINESS_ACCOUNT_ID=your_business_account_id_here
WHATSAPP_API_VERSION=v22.0

# Webhook verification token (set this to any secure random string)
WHATSAPP_VERIFY_TOKEN=your_secure_verify_token_here

# ============================================================================
# 24-Hour Window Configuration
# ============================================================================

# Tracking mode: 'memory' (fast, lost on restart) or 'database' (persistent)
# Recommended: database for production
WHATSAPP_TRACKING_MODE=database

# Template message name (create this in Meta Business Manager first)
# This is used when the 24-hour window has expired
WHATSAPP_DOCUMENT_TEMPLATE_NAME=document_notification

# ============================================================================
# How it works:
# ============================================================================
#
# WITHIN 24 HOURS of company messaging you:
#   → Sends freeform message (your custom notification)
#   → No template needed
#   → More flexible, personalized
#
# OUTSIDE 24 HOURS (or company never messaged):
#   → Sends template message (must be pre-approved by Meta)
#   → Requires WHATSAPP_DOCUMENT_TEMPLATE_NAME to be set
#   → Template must be created and approved in Meta Business Manager
#
# See WHATSAPP_TEMPLATE_SETUP_GUIDE.md for template creation instructions
# ============================================================================
